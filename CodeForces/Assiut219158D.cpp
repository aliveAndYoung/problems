// by LIFECOULDBEDREAM
// 2025/09/19 11:50

#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long a, b, c, d;
    cin >> a >> b >> c >> d;
    long long X = (a * b) - (c * d);
    // X = (A * B) - (C * D)
    cout << "Difference = " << X;

    return 0;
}