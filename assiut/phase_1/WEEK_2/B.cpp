// by LIFECOULDBEDREAM
// 2026/03/19 02:06

#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, q;
    cin >> n >> q;
    vector<int> _v(n + 1, 0);
    for (int i = 1; i <= n; i++)
        cin >> _v[i];
    
    return 0;
}